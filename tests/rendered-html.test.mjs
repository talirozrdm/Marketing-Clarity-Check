import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";
async function render(){const u=new URL("../dist/server/index.js",import.meta.url);u.searchParams.set("test",`${process.pid}-${Date.now()}`);const{default:w}=await import(u.href);return w.fetch(new Request("http://localhost/",{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("עמוד הבית מציג את חוויית V3",async()=>{const r=await render(),html=await r.text();assert.equal(r.status,200);assert.match(html,/מה באמת תוקע/);assert.match(html,/התחילי את האבחון/);assert.match(html,/כ־5 דקות/);assert.match(html,/מפה שמראה מה קודם/);assert.doesNotMatch(html,/בואי נכיר/);assert.doesNotMatch(html,/—/)});
test("מבנה התוצאה כולל את כל שכבות האבחון",()=>{const s=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8");for(const x of["result-hero","evidence section","impact section","focus section","action section","today","month section","final-cta"])assert.match(s,new RegExp(x));assert.match(s,/https:\/\/tali-digicard\.vercel\.app\//)});
test("אין שפה פנימית או מקף ארוך בממשק",()=>{const s=["../app/page.tsx","../app/layout.tsx"].map(x=>fs.readFileSync(new URL(x,import.meta.url),"utf8")).join("\n"),strings=s.match(/"(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|`(?:\\.|[^`\\])*`/g)||[],hebrew=strings.filter(x=>/[א-ת]/.test(x)),forbidden=/—|\b(?:Direction|Capacity|Audience|Offer|Message|Content|Conversion|Reach|Primary|Secondary|Scoring)\b|Rule Engine|Follow-up/i;assert.deepEqual(hebrew.filter(x=>forbidden.test(x)),[])});
