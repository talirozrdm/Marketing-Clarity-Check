import assert from"node:assert/strict";import fs from"node:fs";import test from"node:test";
async function render(){const u=new URL("../dist/server/index.js",import.meta.url);u.searchParams.set("test",`${process.pid}-${Date.now()}`);const{default:w}=await import(u.href);return w.fetch(new Request("http://localhost/",{headers:{accept:"text/html"}}),{ASSETS:{fetch:async()=>new Response("Not found",{status:404})}},{waitUntil(){},passThroughOnException(){}})}
test("עמוד הבית נטען עם המותג וההנעה",async()=>{const r=await render(),html=await r.text();assert.equal(r.status,200);assert.match(html,/מה באמת תוקע/);assert.match(html,/tali-mark\.png/);assert.match(html,/התחילי את האבחון/);assert.doesNotMatch(html,/—/)});
test("קיימים שבעה ויזואלים שונים",()=>{const s=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8");for(const name of["goal","focus","audience","response","message","reach","consistency"])assert.match(s,new RegExp(`area===\\"${name}\\"`));assert.match(s,/HealthMap/)});
test("דשבורד התוצאה כולל את היררכיית המוצר",()=>{const s=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8");for(const section of["result-hero-new","snapshot","why","diagnosis-visual","action-hierarchy","month-plan","avoid-now","final-cta"])assert.match(s,new RegExp(section));assert.match(s,/לבניית תכנית שיווקית יחד/);assert.match(s,/אני ממשיכה לבד עם המפה/)});
test("אין מונחי מנוע באנגלית במחרוזות העבריות",()=>{const s=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8"),strings=s.match(/"(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|`(?:\\.|[^`\\])*`/g)||[];assert.deepEqual(strings.filter(x=>/[א-ת]/.test(x)&&/\b(?:primary|critical|healthy|score|pattern|response|reach)\b/i.test(x)),[])});
